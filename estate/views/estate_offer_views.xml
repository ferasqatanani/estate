<odoo>
  <record id="view_estate_offer_list" model="ir.ui.view">
    <field name="name">estate.property.offer.list</field>
    <field name="model">estate.property.offer</field>
    <field name="arch" type="xml">
      <list>
        <field name="property_id"/>
        <field name="partner_id"/>
        <field name="price"/>
        <field name="status"/>
        <field name="validity"/>
      </list>
    </field>
  </record>

  <record id="view_estate_offer_form" model="ir.ui.view">
    <field name="name">estate.property.offer.form</field>
    <field name="model">estate.property.offer</field>
    <field name="arch" type="xml">
      <form string="Offer">
        <sheet>
          <group>
            <field name="property_id"/>
            <field name="partner_id"/>
            <field name="price" widget="monetary" options="{'currency_field': 'currency_id'}"/>
            <field name="currency_id"/>
            <field name="validity"/>
            <field name="status"/>
          </group>
          <footer>
            <button string="Accept" type="object" name="action_accept" class="btn-primary"
                    modifiers="{'invisible': [('status','!=','new')]}"/>
            <button string="Refuse" type="object" name="action_refuse"
                    modifiers="{'invisible': [('status','=','refused')]}"/>
          </footer>
        </sheet>
      </form>
    </field>
  </record>
</odoo>

